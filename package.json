{
  "name": "requestplus",
  "version": "0.8.2-BETA-PreRe1",
  "dependencies": {
    "body-parser": "^1.20.3",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "net": "^1.0.2",
    "node-fetch": "^3.3.2",
    "openurl": "^1.1.1",
    "tmi.js": "^1.8.5",
    "ws": "^8.18.0"
  },
  "scripts": {
    "test": "nodemon --watch . --exec \"electron .\"",
    "pack": "electron-builder --dir",
    "dist": "node build.js",
    "build": "electron-builder --win --publish never",
    "build:mac": "electron-builder --mac --publish never --universal -c.mac.identity=null"
  },
  "devDependencies": {
    "electron": "^35.1.5",
    "electron-builder": "^26.0.12"
  }
}
